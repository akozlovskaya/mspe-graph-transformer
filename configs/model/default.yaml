# Default model configuration
name: graph_transformer
hidden_dim: 256
num_layers: 6
num_heads: 8
dropout: 0.1
ffn_dim: 512
layer_norm: true
pre_norm: true
residual: true
task: graph  # graph, node, edge
